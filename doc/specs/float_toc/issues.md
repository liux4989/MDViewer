# general
- [ ] the position should be dock at middle - left side of the editor
- [ ] first init the auto-scroll not enable
# detail mode
- [x] auto scroll to the active heading (not needed - detail view closes during scrolling) 
- [x] ellipsis for long headings


# Preview mode

- [ ] remove the container only for preview mode since it is look more like a built-in componet in editor view
- [x] adjust the heading length to each level has same length while different level still has different length since it looks like more clean and unified
- [x] adjust the heading width is not too wide

# Animation
- [ ] the navigation animation is not smooth
- [ ] the scroll animation is not smooth
- [ ] the hover animation is not smooth
- [ ] the inactive animation is not smooth